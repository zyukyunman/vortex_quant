---
tags: [vortex, vortex/architecture]
aliases: [全量开发计划, Development Plan]
created: 2026-04-04
updated: 2026-04-26
---

# 09 - 全量开发计划

> **版本**：v1.2  
> **状态**：执行中  
> **最后更新**：2026-04-07

---

## 1. 目标

从零开始、按域分阶段实现 Vortex 量化研究平台的**全部框架功能**。产品设计（[[产品原型总览]]）和软件架构设计（[[软件架构总览]] ～ [[实例策略开发流程|08]]）中提到的每一项能力都必须落地。

### 1.1 Scope 边界

**在 scope 内的框架基础设施**（虽是具体实现，但属于框架而非用户策略实例）：

| 组件 | 说明 |
|------|------|
| `TushareProvider` | 数据适配器，唯一内置的 DataProvider 实现 |
| `PaperGateway` | 模拟交易网关 |
| 内置 preset / pack | 配置预设包（quality / pit / publish / storage） |
| HTML 报告模板 | 因子评测报告 7 页 + 回测报告 6 页 |
| 内置 Watch Provider | data_health / valuation_percentile / strategy_analysis / factor_monitor |

**不在 scope 内**：

- 具体因子实现（momentum、fcf_yield、low_vol 等）——用户通过 `vortex research register` 注册自己的因子
- 具体策略流水线实例——用户通过 StrategyProfile 配置自己的管线
- MiniQMT 真实网关（仅留 Gateway Protocol + placeholder）
- AI 辅助功能（因子自动分类、regime 标注——标记为 optional/deferred）

---

## 2. 开发节奏

采用渐进式闭环策略：每完成一个步骤即可形成可用闭环，逐步扩大能力范围。

```
Step 1: Phase 0 + 1A  ──→  平台骨架 + 数据采集可用
Step 2: Phase 1B + 2   ──→  数据发布 + 因子构建/评测闭环
Step 3: Phase 5        ──→  通知接入，形成 数据→研究→通知 闭环
Step 4: Phase 3A       ──→  策略核心（管线 + 回测 + 评分）
Step 5: Phase 3B       ──→  策略报告 + 生命周期管理
Step 6: Phase 4        ──→  交易域（模拟/实盘），最终闭环
```

**全程贯穿**：
- **Phase 6（集成测试 + 用户手册）**：每个步骤完成后更新用户手册对应章节，补充端到端测试
- **Phase 5（通知）**：Step 3 搭建后，后续每个域的事件通知持续接入
- **交互式配置 Pipeline**：每个域首次使用时触发交互式配置向导，结果沉淀到 workspace profiles/

### 2.1 步骤依赖图

```
Step 1: [Phase 0] → [Phase 1A]
           ↓
Step 2: [Phase 1B] → [Phase 2]
           ↓
Step 3: [Phase 5A] → [Phase 5B]     ← 数据→研究→通知 闭环
           ↓
Step 4: [Phase 3A]                   ← 策略核心
           ↓
Step 5: [Phase 3B]                   ← 策略报告 + 生命周期
           ↓
Step 6: [Phase 4]                    ← 交易域，全平台闭环
```

---

## 3. Phase 0：平台运行时与控制面

**目标**：搭建所有域共用的基础设施，使后续每个域可以独立开发并接入。

### 3.1 项目骨架

- `pyproject.toml` 依赖确认 + `vortex/` 包初始化
- 模块布局：`vortex/{config,data,research,strategy,trade,notification,runtime,shared}/`
- 日志框架（structured logging, `--verbose` flag）
- 错误码体系（`{DOMAIN}_{CATEGORY}_{DETAIL}` 格式，参见 06 §1.5）
- 确定性约束：
  - 排序规则：date ASC, symbol ASC
  - 精度约定：参见 06 §1.2 精度表
  - 时区：Asia/Shanghai (UTC+8)，参见 06 §1.3

### 3.2 工作区与状态管理

- `vortex init` 命令：创建 workspace 目录结构（参见 06 §8.5 / 00 §6.3）
- **交互式初始化（TTY 环境）**：
- 步骤 1：数据源凭证（检测/配置 TUSHARE_TOKEN + 冒烟测试）
- 步骤 2：历史范围（history_start）
- 步骤 3：是否立即开始首次数据更新；若是，则选择本次先同步的数据集
- 步骤 4：自动调度（更新时间 cron，需内置清晰样例）
- 结果沉淀到 `profiles/default.yaml`
- **非交互模式**：`--non-interactive` 使用全部默认值，适用于 CI / 脚本
- 控制面 SQLite 初始化（`state/control.db`）：所有域共用 DDL 基表
- **Schema 版本管理**：内置 `schema_version` 表 + `_MIGRATIONS` 注册表，启动时自动检测并执行增量迁移，旧库无需手动删除（参见 06 §1.7）
- 数据面目录结构：`data/` `research/` `strategy/` `trade/` 分离（参见 00 §6.2 控制面/数据面分离）
- `run_id` 生成规则：`{domain}_{date}_{time}_{random4}`
- Lineage 基础字段：`parent_run_id` / `created_at` / `vortex_version`
- **默认配置沉淀**：init 完成后 workspace `profiles/` 目录中可见最小用户配置 YAML；provider 默认 datasets 与 quality/pit/publish/storage 内置规则由 resolver 补齐，不直接展开到用户文件
- **Data CLI 口径**：`vortex data bootstrap/update/status/...` 默认使用工作区 `profiles/default.yaml`，不要求用户显式传 `--profile`

### 3.3 配置/Profile 系统

- Profile 基础模型：`BaseProfile` + 4 种子类（DataProfile / ResearchProfile / StrategyProfile / TradeProfile）
- 五阶段管线：Store → Loader → Merger → Validator → Resolver（参见 02 §三-七）
- Preset 系统：内置技术方案包（参见 02 §三.2）
- Pack 系统：可插拔规则包（quality / pit / publish / storage，参见 02 §三.3）
- Profile 继承：`extends` 字段（参见 02 §八.1）
- CLI 命令：`vortex profile explain` / `vortex profile resolve`
- ResolvedProfile 输出：`.vortex/resolved/{name}.resolved.yaml` + 来源标注

### 3.4 服务运行时

- Server 进程：`vortex server start` / `stop` / `status`
- **Graceful Shutdown**：
  - 信号处理：SIGINT / SIGTERM → draining 状态
  - 排空：停止接受新任务，等待运行中任务完成（超时 60s）
  - 超时后标记残留任务为 cancelled + 记录断点
  - PID 文件：`state/vortex.pid`，`server stop` 读取 PID 发送 SIGTERM
  - 崩溃恢复：启动时检测 stale RUNNING 任务 → 标记为 interrupted
- CLI dispatcher → Server RPC 调用链（参见 00 §4 服务化架构）
- 全局任务队列 + 并发控制（`max_concurrent_tasks`，参见 07 §2.8）
- 任务状态机：`pending → running → success / failed / cancelled`
- **任务幂等与互斥**：
  - submit 前检查同 `(domain, profile, resource_key)` 是否有 PENDING/RUNNING 任务
  - DB 事务保证原子性，防竞态
  - resource_key 由 action + 参数决定（如 backfill 的日期范围）
  - dry-run 不创建任务记录
- 任务进度追踪：run / stage / dataset / chunk 四级（参见 06 §3.8）
- 任务取消协议：cancel 信号 + graceful shutdown

### 3.5 事件总线与调度

- 事件 schema：`event_type` / `domain` / `level` / `payload` / `timestamp`
- 事件发布/订阅机制（进程内 bus）
- 调度器框架（cron-like schedule 配置，支持 data auto-run）
- 审计日志基表

### 3.6 跨域交换对象

- `SnapshotDescriptor`（Data → Research/Strategy/Trade，参见 06 §7.1）
- `SignalSnapshotDescriptor`（Research → Strategy，参见 06 §7.2）
- `TargetPortfolio`（Strategy → Trade，参见 06 §7.3）
- `ExecutionReport`（Trade → Audit/Notification）
- 共享工具：交易日历服务、rounding helpers、timezone helpers

### Phase 0 验收标准

- [ ] `vortex init` 创建完整 workspace + 沉淀默认配置
- [ ] `vortex init` 交互模式：TUSHARE_TOKEN 配置 + 数据偏好 + 调度
- [ ] `vortex init --non-interactive` 使用默认值静默完成
- [ ] Profile 全链路（load → merge → validate → resolve）
- [ ] `vortex server start/stop/status` 正常运行
- [ ] Server graceful shutdown（SIGINT/SIGTERM → drain → exit）
- [ ] 任务幂等：重复提交被拒绝
- [ ] Schema 迁移：旧库自动升级，数据不丢失
- [ ] 事件发布/订阅正常
- [ ] 交换对象序列化/反序列化正确

---

## 4. Phase 1A：数据采集与存储层

**目标**：实现数据从外部源到本地存储的完整链路。

### 4.1 Provider 抽象 + TushareProvider

- `DataProvider` Protocol（参见 01 §2 / 06 §3）：
  - `fetch_instruments()` / `fetch_calendar()` / `fetch_bars()` / `fetch_fundamental()` / `fetch_events()`
  - 返回排序约束：date ASC, symbol ASC
- 演进方向：从当前硬编码的 5 类 fetch 过渡到 **dataset registry 驱动的通用分发引擎**，最终接入 Tushare skill 中涉及的全部 **可落盘 Data 域接口**
  - **`dataset_registry` 属性**：Provider 自带完整数据集注册表，描述自己支持的全部数据集
- `TushareProvider` 实现：行情 / 财务 / 事件 / 日历
- **Tushare 数据集注册表**（Phase 1A 实装 + 后续扩展预留）：

| 数据集 | Tushare API | 说明 | Phase |
|--------|-------------|------|-------|
| `instruments` | `stock_basic` | A 股标的列表 | 1A |
| `calendar` | `trade_cal` | 交易日历 | 1A |
| `bars` | `daily` | 日线行情 (OHLCV) | 1A |
| `fundamental` | `income` | 利润表 (PIT) | 1A |
| `events` | `dividend` | 分红数据 | 1A |
| `adj_factor` | `adj_factor` | 复权因子 | 1B |
| `valuation` | `daily_basic` | 估值指标 (PE/PB/市值) | 1B |
| `balancesheet` | `balancesheet` | 资产负债表 | 1B |
| `cashflow` | `cashflow` | 现金流量表 | 1B |
| `fina_indicator` | `fina_indicator` | 财务指标 (ROE/毛利率) | 1B |
| `moneyflow` | `moneyflow` | 个股资金流 | 1B |
| `index_daily` | `index_daily` | 指数日线 | 1B |
| `index_weight` | `index_weight` | 指数成分权重 | 1B |
| `suspend` | `suspend_d` | 停复牌 | 1B |

- DataProfile 改动：`datasets` 默认为 provider 全量注册表；新增 `exclude_datasets` 用于排除
- 分段拉取 + 限流 + 重试（指数退避 5m → 15m → 30m，参见 07 §1.3）

### 4.2 存储层（ODS → DWD → DWS → ADS）

- `StorageBackend` Protocol（参见 06 §3）：`upsert()` / `read()` / `list_partitions()` / `snapshot()`
- Parquet 分区存储（按日期分区）
- DuckDB catalog 维护（查询加速层）
- 四层存储契约（参见 01 §3）：
  - **ODS**（原始层）：Provider 返回的原始数据保留
  - **DWD**（清洗层）：标准化字段、去重、类型转换
  - **DWS**（研究层）：PIT 对齐、衍生指标
  - **ADS**（发布层）：快照冻结、对外交换

### 4.3 数据管线

| Action | 编号 | 说明 | 参见 |
|--------|------|------|------|
| `bootstrap` | D1 | 首次全量初始化 | 06 §三.2 |
| `update` | D2 | 每日增量同步 | 06 §三.3 |
| `backfill` | D3 | 指定日期范围补数 | 06 §三.4 |
| `repair` | D4 | 重试失败 chunk | 06 §三.5 |
| `status` | D6 | 查看数据线健康状态 | 06 §三.7 |

- 管线进度追踪：复用 Phase 0 四级进度（run / stage / dataset / chunk）

### 4.4 质量门禁

- 质量规则引擎（critical / warning 分级，参见 01 §4）
- 内置规则：

| 规则 | 级别 | 说明 |
|------|------|------|
| `not_empty` | critical | 数据集不能为空 |
| `no_all_nan` | critical | 不能全部为空值 |
| `date_continuity` | critical | 交易日连续性 |
| `ohlcv_range` | critical | OHLCV 取值范围合理 |
| `missing_ratio` | warning | 缺失率 < 5% |
| `volume_zero` | warning | 零成交占比 < 10% |

- 质量报告生成

### 4.5 PIT 对齐引擎

- PIT 规则：`effective_from = ann_date + 1 trading day 09:30`（参见 06 §1.3）
- 财务数据 PIT 对齐处理
- PIT 对齐报告

### Phase 1A 验收标准

- [ ] TushareProvider 可拉取行情/财务/日历数据
- [ ] 数据写入四层存储正确
- [ ] `bootstrap` / `update` / `backfill` / `repair` / `status` 正常执行
- [ ] 质量规则触发：critical 阻断 / warning 通过
- [ ] PIT 对齐正确性验证

---

## 5. Phase 1B：数据发布与版本管理

**目标**：实现快照发布、版本管理、GC 及 Qlib 视图。

### 5.1 快照发布（D5: publish）

- 质量门禁通过 → PIT 对齐 → 快照冻结 → `SnapshotDescriptor` 生成
- 衍生指标计算（估值百分位、市场宽度等，参见 06 §三.6）
- 数据健康报告

### 5.2 快照版本模型

- 逻辑快照 + 物理修订（revision）（参见 00 §6.1）
- 同一 `as_of` 可重新发布（新 revision 替代旧 revision）
- 下游引用 `snapshot_revision_id`
- 旧 revision 保留直到 GC

### 5.3 Qlib 增量视图

- Qlib URI 格式数据视图构建
- 增量更新（仅追加新数据）

### 5.4 GC + 数据健康报告

- `vortex data gc`：清理未引用的已替代修订
- 数据健康报告（覆盖率、连续性、质量统计）

### 5.5 数据域 CLI 完整命令

```
vortex data bootstrap  --profile <name> [--verbose] [--dry-run]
vortex data update     --profile <name>
vortex data backfill   --profile <name> --start <date> --end <date>
vortex data repair     --profile <name> --run-id <id>
vortex data publish    --profile <name>
vortex data status     --profile <name> [--format json]
vortex data gc         --profile <name> [--dry-run]
```

### Phase 1B 验收标准

- [ ] 快照发布完整链路
- [ ] 版本管理（重发布 + revision 引用）
- [ ] Qlib 视图可被下游读取
- [ ] GC 正确清理未引用 revision
- [ ] CLI 全命令可执行

---

## 6. Phase 2：研究域

**目标**：实现因子注册、因子构建、预处理、评测引擎、分类型评测、HTML 报告、信号发布的完整研究闭环。

### 6.1 因子注册与管理

- `FactorRegistry`：register / unregister / retype / list-factors（参见 03 §2）
- `FactorSource`：字段 / 公式 / 代码三类来源
- `FactorBuildService`：Research 内部构建编排，不单独暴露 CLI
- `DataAPIReader` + `SQLReader`：默认取数主路径
- `FormulaEngine` + `PythonFactorLoader`：公式执行与复杂逻辑扩展口
- `OperatorBackend`：`native / talib / pandas_compat`
- `Factor` Protocol：`name` / `category` / `direction` / `compute(data) -> Series`
- 8 种因子类型：value / momentum / quality / volatility / liquidity / technical / alternative / composite
- 因子缓存 + `code_hash` 自动失效
- `factor_archive`：评测历史（append-only）
- 去重机制：`eval_key = (factor_name, snapshot, market, label_spec, code_hash)`

### 6.2 因子构建、预处理与通用评测引擎（L1）

参见 03 §4.1-4.6、附录《因子研究与评测全流程说明》/ 06 §4.3：

- 构建层支持字段因子、公式因子、代码因子三种输入
- 构建阶段不单独暴露 CLI，由 `research run` 内部调用
- 预处理默认包含：研究样本过滤、MAD 去极值、z-score、行业中性化、市值中性化（可选）、最终标准化
- 每次运行产出 `factor_build_manifest` 与 `preprocess_report`

| 维度 | 指标 |
|------|------|
| 预测力 | IC / RankIC / ICIR |
| 信号持续性 | IC 衰减曲线（1d / 5d / 20d） |
| 分层回测 | 分组收益（5组/10组）/ 多空年化收益 / 单调性 |
| 风险调整收益 | Sharpe / Information Ratio / Max Drawdown / Alpha-Beta |
| 覆盖率 | 因子覆盖率 |
| 稳定性与鲁棒性 | 因子自相关 / 子区间稳定性 / Regime / 参数扰动 / OOS 衰减 |
| 正交性 / 独立性 | 残差 IC / 因子相关矩阵 / 增量贡献 |
| 分布 | 截面分布统计 |

### 6.3 分类型评测（L2-L4）

参见 03 §4.8 / 06 §4.4：

- **L2 CategoryMetric**：按因子类型有条件计算（10 个 category metric）
- **L3 ScoringPolicy**：百分位排名 + 加权评分（5 维度 × 8 类型权重表）
- **L4 EvalEmphasis**：每种因子类型的展示侧重配置（sidebar / highlighted / hints / panels）
- 评分等级：A ≥ 85 / B+ ≥ 75 / B ≥ 70 / B- ≥ 60 / C ≥ 50 / D ≥ 35 / F < 35

### 6.4 HTML 因子评测报告（7 页）

对齐产品原型 `factor_tear_sheet.html`，payload 遵循 06 §4.8，渲染契约遵循 06 §4.9：

| 页面 | 内容 |
|------|------|
| IC 分析 | IC 时序 + 衰减曲线 + 累计 IC |
| 分组收益 | 分组净值 + 多空 + 年度分层 |
| 截面分布 | 稳定性热力图 + 分布变化 |
| 截面快照 | 时间滑块回放 + 散点图 + 回归线 |
| 相关性矩阵 | 因子间相关 + 增量 IC + 市值相关 |
| Regime 分析 | 分市场环境 IC + 切换期表现 |
| 容量分析 | 分市值 IC + 换手/成本 + 敏感度 |

### 6.5 信号发布

- `publish-signal`：实验 → 不可变信号快照（参见 03 §2.10 / §3.1）
- `signal_id` 格式：`{factor_family}_{variant}_{version}`
- **不可变规则**：发布后永远不可修改/覆盖，修复必须新建 signal_id
- **硬约束**：model_train 不能直接 publish-signal，必须经过 factor_eval

### 6.6 模型训练工作流

- Qlib 适配层（ModelTrainer，参见 03 §3）
- 训练/验证分离 + 过拟合检测
- 特征重要性报告
- 输出 `pred.pkl` → 走 factor_eval 管线

### 6.7 研究域 CLI 完整命令

```
vortex research register       --name <name> --source <code|table|formula> [--path <path> | --formula <expr>]
vortex research unregister     --name <name>
vortex research retype         --factor <name> --factor-type <type>
vortex research list-factors   [--status active|inactive]
vortex research archive        --factor <name> [--limit N]
vortex research run            --profile <name> --snapshot <id> --factor <name>
vortex research inspect        --run-id <id>
vortex research compare        --run-id <id1> <id2>  |  --factors <f1> <f2>
vortex research publish-signal --run-id <id> [--tag <tag>]
vortex research status         --profile <name>
```

### Phase 2 验收标准

- [ ] 因子注册/注销/retype 生命周期正确
- [ ] 因子构建链路（API / SQL / Python 扩展口）可用
- [ ] 预处理 pipeline（样本过滤 / 去极值 / 标准化 / 中性化）结果正确
- [ ] 通用评测指标计算结果正确（合成数据验证）
- [ ] 风险调整收益、鲁棒性与正交性链路正常
- [ ] L2-L4 分类型评测链路正常
- [ ] HTML 报告生成 + payload 符合 06 §4.8 schema
- [ ] 信号不可变性 + publish 工作流
- [ ] eval_key 去重
- [ ] CLI 全命令可执行

---

## 7. Phase 5：通知域

> **启动时机**：Phase 2 完成后立即启动，形成数据→研究→通知闭环。后续域的事件通知持续接入。

### 7.1 Phase 5A：事件通知与路由

- `NotificationChannel` Protocol：`send(message) -> bool` / `is_available() -> bool`
- Feishu 通道实现
- 事件类型 → 级别（critical / warning / info）→ 通道路由
- 结构化通知内容（what / impact / action / run_id，参见 07 §5.3）
- 通知模板（参见 07 §5.2）：

| 事件 | 级别 | 说明 |
|------|------|------|
| 数据质量阻断 | critical | 质量门禁未通过 |
| 数据源不可用 | critical | Provider 连续失败 |
| A 级因子发现 | info | 新评测获 A 评级 |
| F 级回测警告 | warning | 回测质量极差 |
| 风控阻断 | critical | pre-trade 拦截 |
| 执行完成 | info | 交易执行结束 |
| 对账异常 | critical | 偏差超阈值 |

- 安静时段配置（仅 critical 在非工作时间发送）
- 通知历史记录
- CLI：`vortex notify test` / `vortex notify history`

### 7.2 Phase 5B：Watch 订阅系统

- `WatchProvider` Protocol：`pull(context) -> WatchResult`（参见 07 §5.5）
- Watch 订阅存储（数据库，非配置文件）
- 调度触发（daily_after_publish 等）
- 失败处理 + 连续 3 次失败自动暂停
- 内置 Watch Provider：

| Provider | 频率 | 说明 |
|----------|------|------|
| `data_health` | 每日 | 数据健康摘要 |
| `valuation_percentile` | 每日 | 估值百分位推送 |
| `strategy_analysis` | 每周 | 策略分析周报 |
| `factor_monitor` | 每日 | 因子监控 |

- CLI：`vortex watch add` / `list` / `remove` / `pause`

### Phase 5 验收标准

- [ ] Feishu 通知发送成功
- [ ] 事件路由 + 级别过滤正确
- [ ] 安静时段逻辑生效
- [ ] Watch 调度正常 + 失败自动暂停
- [ ] CLI 全命令可执行

---

## 8. Phase 3A：策略核心

**目标**：实现管线、回测、评分、编译的策略核心能力。

### 8.1 四阶段管线

`PipelineStage` 契约（参见 04 §2-4 / 06 §5.2）：

| 阶段 | 职责 | 输出 |
|------|------|------|
| Universe | 标的池筛选（指数成分/流动性/上市天数/排除ST） | `symbols: list[str]` |
| Alpha | 信号融合（加权融合 / 级联模式） | `scores: dict[str, float]` |
| Portfolio | 权重分配（等权/信号加权/风险平价） | `holdings: list[Holding]` |
| Risk | 约束检查（持仓上限/行业暴露/回撤止损） | `holdings + adjustments` |

- 管线配置模型（StrategyProfile）
- 阶段独立测试（`--stage` + `--dry-run`）

### 8.2 回测引擎

- `BacktestEngine` 适配层（可插拔后端，参见 06 §5.3）
- 成本模型：`CnStockDefaultCost`（3bps 佣金 + 1‰ 印花税）
- 换仓频率配置（daily / weekly / monthly）
- 换手率计算（年化双边）

### 8.3 多维度回测

参见 04 §5 / 07 §4.4：

| 维度 | 目的 | 关键参数 |
|------|------|----------|
| 全样本 | 基线表现 | 完整回测区间 |
| 随机日期 | 起始日期鲁棒性 | `n_trials=20`, `min_window=252d` |
| Walk-Forward | 过拟合检验 | `train=252d`, `test=63d`, `step=63d` |
| Regime | 市场环境拆分 | MA120 判断牛/熊/震荡 |

### 8.4 双基准系统

参见 04 §6 / 07 §4.5：

- **指数基准**：直接价格序列
- **Alpha 基准**：成分股复制（季度 / 动态权重模式）
- 基准数据必须来自 PIT 对齐的 Data snapshot

### 8.5 质量评分

参见 04 §7 / 07 §4.3：

| 维度 | 权重 | 公式核心 |
|------|------|----------|
| 收益风险 | 25% | Sharpe bootstrap p-value |
| 稳定性 | 25% | Rolling 6M Sharpe std |
| 鲁棒性 | 20% | WF OOS/IS Sharpe ratio |
| 成本效率 | 15% | (Return - Cost) / Turnover |
| 过拟合风险 | 15% | Full vs Random vs WF degradation |

等级：A ≥ 85 / B+ ≥ 75 / B ≥ 70 / B- ≥ 60 / C ≥ 50 / D ≥ 35 / F < 35

### 8.6 组合编译

- `compile`：信号 → 目标组合（`target_portfolio.json`）
- 幂等性：`portfolio_id = tp_{profile}_{snapshot_hash_6}_{signals_hash_6}`
- 组合状态：draft → frozen → superseded

### Phase 3A 验收标准

- [ ] 四阶段管线执行正确
- [ ] 阶段独立测试可用
- [ ] 4 种维度回测均可运行
- [ ] 双基准计算正确
- [ ] 质量评分 + 等级判定
- [ ] 组合编译幂等性

---

## 9. Phase 3B：策略生命周期与报告

### 9.1 结果生命周期管理

参见 07 §4.7-4.8：

```
active ←→ protected（tag 保护）
  ↓
archived（30 天恢复窗口）
  ↓
purged（仅保留 manifest）
```

- `tag`：标记重要回测
- `restore`：30 天内恢复
- `cleanup`：智能分层清理建议

### 9.2 HTML 回测报告（6 页）

对齐产品原型 `backtest_report.html`，payload 遵循 06 §5.7：

| 页面 | 内容 |
|------|------|
| Dashboard | 核心指标 + 质量雷达图 + 净值曲线 |
| 月度热力图 | 月度/年度收益 + 超额 |
| 滚动指标 | Sharpe / Beta / 波动率 / 最大回撤 时序 |
| 风险归因 | 行业/风格暴露 + 集中度 |
| 交易分析 | 持仓周期 / 盈亏分布 / 回撤连续 |
| 管线分解 | 阶段贡献 NAV 拆分 |

### 9.3 策略域 CLI 完整命令

```
vortex strategy backtest  --profile <name> --snapshot <id> --signal <id>
vortex strategy inspect   --run-id <id>
vortex strategy compare   --left <id> --right <id>
vortex strategy compile   --profile <name> --snapshot <id> --signal <id>
vortex strategy signal    --profile <name>
vortex strategy tag       --run-id <id> --tag <tag>
vortex strategy restore   --run-id <id>
vortex strategy cleanup   --profile <name> [--dry-run]
vortex strategy status    --profile <name>
```

### Phase 3B 验收标准

- [ ] 生命周期管理（tag/archive/restore/cleanup/purge）
- [ ] HTML 报告与产品原型对齐
- [ ] CLI 全命令可执行

---

## 10. Phase 4：交易域

**目标**：实现模拟/实盘交易的完整链路，这是全平台最终闭环。

### 10.1 网关抽象

- `Gateway` Protocol（参见 05 §3 / 06 §6）：
  - `connect()` / `submit_order()` / `cancel_order()` / `query_positions()` / `query_orders()` / `on_fill()`
- `PaperGateway` 实现：FIFO 撮合、涨跌停限制、停牌拒绝
- `MiniQMTGateway` placeholder（仅接口定义）

### 10.2 订单生命周期

- 状态机：`created → submitted → [partial_fill / filled / rejected / timeout / cancelled]`
- 终态：filled / rejected / timeout / cancelled
- `OrderIntent` 控制面持久化（先写 intent 再提交）
- `client_order_id`：`{exec_id}_{seq}` 确定性生成
- 幂等性：canonical hash 指纹冲突检测

### 10.3 执行引擎

- 订单计划生成（目标组合 diff 当前持仓）
- 提交 → 监控 → 成交回报处理
- 超时/拒绝处理

### 10.4 三层风控

参见 05 §4：

| 层 | 触发时机 | 规则示例 | 处置 |
|----|----------|----------|------|
| Pre-Trade | 提交前 | 集中度/涨跌停/ST | **Fail-Closed** |
| Intra-Trade | 每笔成交 | 滑点异常/成交量异常 | 取消剩余 |
| Post-Trade | 日终 | 持仓偏差/价格偏差 | 日志 + 通知 |

- 风控规则 YAML 配置（`RiskRule` Protocol）

### 10.5 对账

- 三组比对（参见 05 §5）：
  1. actual（网关）vs target（组合）
  2. target vs fills（成交日志）
  3. fills vs actual（一致性）
- 阈值：< 1% 记录 / 1-3% 告警 / > 3% 异常

### 10.6 交易域 CLI 完整命令

```
vortex trade paper-rebalance  --profile <name> --target <portfolio_id>
vortex trade live-rebalance   --profile <name> --target <portfolio_id> --confirm
vortex trade inspect          --run-id <id>
vortex trade retry            --run-id <id>
vortex trade reconcile        --profile <name> --date <date>
vortex trade cancel           --run-id <id>
vortex trade status           --profile <name>
```

### Phase 4 验收标准

- [ ] PaperGateway 撮合逻辑正确（正常/涨跌停/停牌）
- [ ] 订单状态机全路径
- [ ] 三层风控触发
- [ ] 对账三组比对
- [ ] 幂等性 + 恢复流程
- [ ] CLI 全命令可执行

---

## 11. Phase 6：集成测试与用户手册（全程贯穿）

### 11.1 端到端冒烟测试

- 完整链路：data bootstrap → publish → research register → run → publish-signal → strategy compile → backtest → trade paper-rebalance → notification
- 验证跨域交换对象正确传递
- 验证 HTML 报告生成

### 11.2 用户使用手册

随每个步骤持续更新，最终形成完整手册：

| 章节 | 对应阶段 | 内容 |
|------|----------|------|
| 安装与初始化 | Step 1 | pip install + vortex init + 配置 |
| 数据管理 | Step 1 | Tushare 配置 → 拉取 → 发布 |
| 因子研究 | Step 2 | 注册因子 → 评测 → 解读报告 → 发布信号 |
| 通知配置 | Step 3 | 通知通道 → Watch 订阅 |
| 策略回测 | Step 4-5 | 管线配置 → 回测 → 解读报告 → 管理结果 |
| 模拟交易 | Step 6 | 组合编译 → 模拟执行 → 对账 |
| 附录 | 全程 | 配置参考 / 错误码 / CLI 速查 |

### 11.3 AI 辅助（optional / deferred）

- 因子自动分类
- Regime 自动标注
- 异常诊断
- LLM 后端配置

---

## 12. 完整 CLI 命令清单

| 域 | 命令 | 数量 |
|----|------|------|
| system | `vortex init` / `vortex server start\|stop\|status` | 4 |
| profile | `vortex profile explain` / `resolve` | 2 |
| data | `bootstrap` / `update` / `backfill` / `repair` / `publish` / `status` / `gc` | 7 |
| research | `register` / `unregister` / `retype` / `list-factors` / `archive` / `run` / `inspect` / `compare` / `publish-signal` / `status` | 10 |
| strategy | `backtest` / `inspect` / `compare` / `compile` / `signal` / `tag` / `restore` / `cleanup` / `status` | 9 |
| trade | `paper-rebalance` / `live-rebalance` / `inspect` / `retry` / `reconcile` / `cancel` / `status` | 7 |
| notify | `test` / `history` | 2 |
| watch | `add` / `list` / `remove` / `pause` | 4 |
| **合计** | | **45** |

---

## 13. 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-04-07 | v1.2 | 新增：Schema 版本管理与增量迁移机制，Init 交互优化（Cron 选项化） |
| 2026-04-07 | v1.1 | 新增：交互式初始化、Provider 数据集注册表、Graceful Shutdown、任务幂等 |
| 2026-04-07 | v1.0 | 初始版本，完整 6 Phase 计划 |
